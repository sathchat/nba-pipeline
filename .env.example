# This project does not require a .env by default.
# Optional toggles (uncomment and set in the workflow env if you like):

# USE_PARQUET=1      # also write export/*.parquet alongside CSVs
# DAYS_BACK=1        # (not used in the default script; left here for extension)
